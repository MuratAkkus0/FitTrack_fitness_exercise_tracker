{
  "name": "fit-track",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'PHP project - no build needed'",
    "vercel-build": "echo 'PHP project - no build needed'"
  },
  "engines": {
    "node": "18.x"
  }
}
